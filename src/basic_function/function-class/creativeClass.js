// 创建一个构造函数（需要传参数的）
// 创建一个Preson（人），这个人可以
class Person {
  // 定义基本属性
  constructor(name, age, sex) {
    // 全部使用传出的内容
    this.name = name;
    this.age = age;
    this.sex = sex;
  }
  // 创建相关功能
}
